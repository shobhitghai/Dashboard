1. Logout functionality to be completed, logout button inside the burger icon. 
2. On clicking the burger icon, when a dropdown/slider opens, it should mention the name of the user.
3. The current selection made in filter panel should be visible in the top bar. 
    - If multiple stores selected, it should show first couple and then ...
    - If no selection made, it should show "All Stores"
4. The information/help icon is needed in All sections on top right of each section. On clicking, content should show up. Will share content by later today. You can start by putting dummy content. 


F. Cross Store Duration 
1. Change the chart based on new SQL queries shared. 